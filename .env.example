# Convex Configuration (for cloud deployment)
# Get this URL by running: npx convex dev
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Deployment configuration (automatically generated by convex dev)
# CONVEX_DEPLOYMENT=dev:your-deployment-id

# Encryption Key (REQUIRED for production)
# Generate with: npm run generate-key
# IMPORTANT: Keep this secret and backed up securely!
# ENCRYPTION_KEY=your-generated-encryption-key-here

# Deployment Mode (optional)
# cloud = Use Convex cloud backend (default)
# docker = Use local SQLite database
# PUBLIC_DEPLOYMENT_MODE=cloud

# Demo Mode (optional, for cloud deployments)
# Set to "true" to enable demo mode features
# PUBLIC_DEMO_MODE=false
